import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.JuriSynk',
  appName: 'JuriSynk',
  webDir: 'dist/argon-dashboard-angular',
};

export default config;
